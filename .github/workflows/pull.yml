on:
  push:
    branches:
    - main

jobs:
  pull-request-develop:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: pull-request
      uses: repo-sync/pull-request@v2
      with:
        destination_branch: [develop, feature]